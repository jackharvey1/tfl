/* global sandbox, expect */
const mongoose = require('mongoose');

describe('Database', function() {
    describe('when getting stations', function() {
        xit('should not produce duplicates', function () {

        });
    });
});
